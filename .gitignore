node_modules/
build/
dist/
coverage/
*.tsbuildinfo

# Banco local. O schema versionado esta em database/schema.sql.
database/*.db
database/*.db-journal
database/*.db-wal
database/*.db-shm

# Banco usado pela suite de testes. Apagado e recriado a cada execucao por
# src/testes/preparacao.ts, e por isso nunca deve ser versionado.
.tmp/
test.db

.env
.env.*
!.env.example

# Logs e temporarios
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Diario pessoal de fases. Fica local de proposito: o registro que vai para o
# GitHub sao os ADRs em docs/adr/, nao o roadmap de trabalho em andamento.
UPGRADES.md
